[project]
name = "interview-inference"
version = "0.1.0"
description = "Modal video transcode service for interview template."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "modal>=0.63.0",
  "python-multipart>=0.0.9",
]

[tool.uv]
package = false
